[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "expense-tracker"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["rich"]

[project.scripts]
xpense = "main:main"
